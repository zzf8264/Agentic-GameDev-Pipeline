# =============================================================================
# Agentic GameDev Pipeline — Runtime Dependencies
# =============================================================================

# --- LLM Client ---
openai>=1.6.0,<2.0               # AsyncOpenAI — any OpenAI-compatible endpoint
tenacity>=8.2,<9.0               # Retry with exponential backoff

# --- Optional: accurate token counting (falls back to 4-char heuristic) ---
tiktoken>=0.5.0,<1.0

# --- Scout Agent ---
playwright>=1.40,<2.0            # Headless Chromium scraping

# --- API server (optional) ---
fastapi>=0.109.0                 # Expose pipeline endpoints
uvicorn[standard]>=0.25.0        # ASGI server

# --- Data / Config ---
pyyaml>=6.0,<7.0                 # Prompt template loading
pydantic>=2.0,<3.0               # Data validation

# --- CLI / Dev UX ---
rich>=13.7,<14.0                 # Pretty logging output
